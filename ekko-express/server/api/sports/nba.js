/**
 * Main application routes
 */

'use strict';

var ExpressStormpath = require('express-stormpath');

module.exports = function (app) {
    // Insert routes below
    var db = app.get('db');
    
};